# R ggplot绘图

## 学习目标

学会用 ggplot2 画散点图。

---

## 代码

```r
library(ggplot2)

ggplot(df, aes(x=logFC, y=-log10(pvalue))) +
  geom_point()
```

---

## 解释

```
ggplot() 创建图
aes() 设置坐标轴
geom_point() 画散点
```

---

## 练习

```r
ggplot(df, aes(x=gene, y=count)) +
  geom_bar(stat="identity")
```