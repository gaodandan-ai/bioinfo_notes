一个细胞
↓
两个 sgRNA
↓
同时抑制两个基因
↓
观察组合效应

用于研究
	遗传互作
	合成致死
	代谢通路关系

# plasmid library结构

R - S1 -R -S2 - R
```
Promoter
   │
   ▼
repeat - spacer1 - repeat - spacer2 - repeat
# repeat CRISPR repeat
# spacer sgRNA targeting sequence
# spacer1 gene1
# spacer2 gene2
```


### PCR扩增出来的结构

PCR引物通常在
	vector region  --  repeat=spacer=repeat=spacer=repeat --  vector region

产物大概是
	adapter -- primer -- repeat spacer repeat spacer repeat -- primer  -- adapter

长度大概200 bp

### Illumina测序读到的
```
ACGTAGTC...
repeat
spacer1
repeat
spacer2
repeat
GATCAGTA...
```

实际可能会有方向的偏转或者位置有一点偏移

```bash
conda activate crispri
cd /data/gaodd/CRISPRi_thermal/crispri_screening_pipeline
```

```bash
nohup python scripts/dual_gene/crispri_dual_gene_analysisi_merged.py \
    --rawdata-dir /data/gaodd/CRISPRi_thermal/raw_data/dual_gene/merged \
    --reference-fasta /data/gaodd/CRISPRi_thermal/crispri_screening_pipeline/data/library3_slt.fasta \
    --anno-file /data/gaodd/CRISPRi_thermal/crispri_screening_pipeline/data/cgl-anno.csv \
    --output-dir /data/gaodd/CRISPRi_thermal/dual_gene_results_merged \
    --threads 8 \
> run.log 2>&1 &


tail -f run.log  #查看日志

```
