Obsidian（写笔记）
        ↓
Git（版本管理）
        ↓
GitHub（云端同步）

每次打开git bash，先输入
```
#进入我的笔记目录
cd /f/bioinfo_notes
```

每次整理完笔记后保存：
```bash
git add .
git commit -m "update notes"
git push
```


```bash
git config --global user.email "gdandan756@gmail.com"
  git config --global user.name "gaodandan-ai"

```